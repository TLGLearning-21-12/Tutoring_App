import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  mainContainer: {
    display: "absolute",
    alignItems: "center",
  },
  smMargin: {
    margin: theme.spacing(3),
  },
  actionDiv: {
    textAlign: "center",
  },
}));
